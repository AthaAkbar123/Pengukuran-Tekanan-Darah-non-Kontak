1. Buat .py untuk menyimpan `.npy` mean rgb dari setiap ROI ke dalam folder subject.
2. Buat .py baru untuk processing. Tahapannya:
    2.1 Load NPY setiap ROI
    2.2 Load GT (SBP dan DBP) dari csv
    2.3 Pre-process signal. Ada dua opsi
        - Opsi 1: dari Mean RGB -> POS (Pakai Kode POS)
        - Opsi 2: Pakai `process_signal` dari ifile `zhou_method.py` yang baru
    2.4 Deteksi Peak and Valley
    2.6 Hitung `e_peak` dan `e_valley`
    2.7 Hitung SBP dan DBP, lalu hitung errornya berdasarkan GT